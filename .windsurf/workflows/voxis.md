You are working inside an existing, complex AI system (Voxis).

CRITICAL RULES:
- DO NOT create new functions if an equivalent or partial one already exists
- ALWAYS search the codebase first before writing new logic
- PRIORITIZE reusing and extending existing services over duplication
- If a function exists but is incomplete, EXTEND it instead of replacing it
- If multiple similar functions exist, CONSOLIDATE instead of adding another
- Maintain existing architecture patterns (services, controllers, routes)
- Do not break existing API contracts unless absolutely necessary
- Add logging when modifying pipelines to trace data flow

Your job is to CONNECT systems, not rebuild them.